<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:gravity="center_vertical"
    android:orientation="vertical"
    android:padding="14dp">

    <TextView
        android:id="@+id/widget_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:contentDescription="Open Budget DAWGs"
        android:fontFamily="sans-serif-medium"
        android:text="@string/widget_title"
        android:textColor="@color/widget_text"
        android:textSize="16sp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="10dp"
        android:layout_weight="1"
        android:gravity="center"
        android:orientation="horizontal">

        <Button
            android:id="@+id/add_expense"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_marginEnd="5dp"
            android:layout_weight="1"
            android:backgroundTint="@color/expense_button"
            android:contentDescription="Add expense"
            android:minHeight="48dp"
            android:text="@string/add_expense"
            android:textAllCaps="false"
            android:textColor="@android:color/white"
            android:textSize="14sp" />

        <Button
            android:id="@+id/add_income"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_marginStart="5dp"
            android:layout_weight="1"
            android:backgroundTint="@color/income_button"
            android:contentDescription="Add income"
            android:minHeight="48dp"
            android:text="@string/add_income"
            android:textAllCaps="false"
            android:textColor="@color/income_button_text"
            android:textSize="14sp" />
    </LinearLayout>
</LinearLayout>
